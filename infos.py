name = input('type your name: ')
age = int(input('type your age: '))
weight = float(input('type your weight: '))
programmer = bool(input('type True for (Im a programmer) and False for (Im not a programmer):'))

print(f'name: {name} \nage: {age} \nweight: {weight} \nprogrammer: {programmer}.')

